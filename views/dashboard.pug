extends layout

block content
  h1 Dashboard
  p Welcome to your dashboard!

  // Displaying videos uploaded by the user
  if videos.length
    h2 Your Videos
    ul
      each video in videos
        li
          h3= video.title
          a(href=video.url, class='watch-video-link') Watch Video
  else
    p You have not uploaded any videos yet.

  // Link to add a new video with enhanced styling
  a(href="/video/new_video", class='add-video-link') Add a New Video

  